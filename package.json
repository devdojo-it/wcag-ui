{
  "name": "wcag-ui",
  "version": "0.0.14",
  "description": "wcagUI Library",
  "repository": "git@github.com:devdojo-it/wcag-ui.git",
  "author": "Emiliano Pisu <pisuemiliano.1980@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clear:cache": "rm -rf .parcel-cache dist",
    "clear": "yarn clear:cache && rm -rf node_modules yarn.lock package-lock.json",
    "reset": "yarn clear && yarn",
    "start": "parcel ./src/index.html",
    "restart": "yarn clear:cache && yarn start",
    "prebuild": "yarn clear:cache",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "parcel build ./src/index.html --no-source-maps --public-url ./",
    "build:push": "git add . && git commit --amend --no-edit && git push -f",
    "deps": "yarn reset && yarn upgrade-interactive --latest",
    "changelog": "changelog -t $(git log --grep='rel(' --format='%H' --max-count=2 | head -n 2 | tail -1)..$(git log --grep='rel(' --format='%H' --max-count=2 | head -n 1) -x rel,build && git add . && git commit --amend --no-edit",
    "rel:major": "lerna publish major -y && yarn deps && yarn build && yarn version --major && yarn changelog",
    "rel:minor": "lerna publish minor -y && yarn deps && yarn build && yarn version --minor && yarn changelog",
    "rel:patch": "lerna publish patch -y && yarn deps && yarn build && yarn version --patch && yarn changelog"
  },
  "workspaces": [
    "packages/**/*"
  ],
  "dependencies": {
    "@wcag-ui/accordion": "^0.0.7",
    "@wcag-ui/button": "^0.0.7",
    "@wcag-ui/checkbox": "^0.0.7",
    "@wcag-ui/core": "^0.0.7",
    "@wcag-ui/details": "^0.0.7",
    "@wcag-ui/grid-system": "^0.0.7",
    "@wcag-ui/iconography": "^0.0.7",
    "@wcag-ui/input": "^0.0.7",
    "@wcag-ui/radio": "^0.0.7",
    "@wcag-ui/scroll-spy": "^0.0.7",
    "@wcag-ui/switch": "^0.0.7",
    "@wcag-ui/typography": "^0.0.7",
    "highlight.js": "11.11.1"
  },
  "devDependencies": {
    "jsdoc": "4.0.4",
    "lerna": "8.2.2",
    "parcel": "2.15.1",
    "posthtml": "0.16.6",
    "posthtml-extend": "0.6.5",
    "posthtml-highlight": "3.0.0",
    "posthtml-include": "2.0.1",
    "posthtml-markdownit": "3.1.1",
    "svgo": "3.3.2",
    "generate-changelog": "^1.8.0",
    "realfavicon": "^0.4.6",
    "yarn":"1.22.22"
  }
}
