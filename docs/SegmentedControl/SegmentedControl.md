# SegmentedControl

The **SegmentedControl** component is a reusable, accessible UI element.

## Usage

```html
<element is="wcag-segmented-control"></element>
```

## WCAG Accessibility

- Uses semantic HTML elements.
- Supports ARIA attributes where appropriate.
- Keyboard navigation enabled.
- Focus management compliant with WCAG 2.1.

## Examples

```html
<!-- Example usage -->
<element is="wcag-segmented-control"></element>
```
